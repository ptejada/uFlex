1. Before you move on reading please notice that this is a class for developers not for end-users
so i won't be spoon feeding steps.

2. This Class relies on MySQL database to work. In the source folder you will find the starting
uFlex database which is a simple user table.

3. The class doesn't handle with the SQL connection so a connection with your database most be 
stablished before creating the uFlex object on your pages. 

4. Read the Documentation in the doc folder to get started.
